Qt ARMSim develpment commands
=============================

To create a source distribution and upload it to the testpypi
repository::

   $ python3 ./setup.py qtclean qtcompile sdist upload -r testpypi

